#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>

int fan_speed = 0;
void change_fan_speed(void);
int main(int argc, char argv[]) {
    char * pAlarm = NULL;
    change_fan_speed();
    while(1) {
    ;
    }
}
